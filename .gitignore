# Worktrees
.worktrees/

# Rust
/target/
**/target/
**/*.rs.bk
# Cargo.lock IS committed (this is a binary application) so builds are
# reproducible across environments and contributors get the same
# transitive dep versions for the security/audit story.

# Tauri (Phase 3)
crates/app/src-tauri/target/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Personal Claude Code state (local-only, per-clone)
.claude/settings.local.json
**/CLAUDE.local.md

# Personal scratch / planning notes
tasks/
